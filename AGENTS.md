# Versioning Rules
When adding new features or fixing bugs in this project, you must automatically increment the application version number in `index.html` (inside the Settings/About container) and `package.json`.
The project is named **MIDIcontrolz2**. 
Follow Semantic Versioning (v2.X.X). The base version started at v2.0.0.
